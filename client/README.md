# @muse/client

Muse 前端（React 19 + Vite + Tailwind CSS + TanStack Query）。

- 开发启动：`pnpm dev`（根目录）；单独 `cd client && npm run dev`
- 构建：`npm run build`（tsc -b + vite build）
- 测试：`npx vitest --run`；类型检查：`npx tsc --noEmit`
- lint：oxlint（`npm run lint`）

路由、Tab、状态管理说明见仓库根 CLAUDE.md。
