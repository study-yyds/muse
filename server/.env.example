# ============ 数据库 (Neon) ============
DATABASE_URL=postgresql://<用户名>:<密码>@<host>/muse?sslmode=require

# ============ JWT ============
JWT_SECRET=change-me-to-a-random-secret

# ============ 服务端口 ============
PORT=3001
